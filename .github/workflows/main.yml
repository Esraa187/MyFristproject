on:
  push: 
    branches:  
      - master

jobs:
  build:
    runs-on: ubuntu-latest


    steps:

      -  uses: actions/checkout@v3
      
      -  name: "Login To Dockerhub"
         run: docker login --username esraa123 --password dckr_pat_7ZrhVSTjqF0AOjESa3_S_DMnwxg
